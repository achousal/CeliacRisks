# Data Directory

This directory is intended for input data files.

Place your dataset files here before running the analysis pipeline.

Note: Data files are not tracked in version control. See `.gitignore` for exclusion patterns.
